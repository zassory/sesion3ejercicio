

export const mostrar = (arreglo = []) => {
    for(let i = 0;i < arreglo.length;i++){
        arreglo[i];
    }
    return arreglo;
}

export const mostrarIngredientesExtras = (ingredientes = []) => {
    for(let i = 0;i < ingredientes.length;i++){
        ingredientes[i];
    }
    return ingredientes;
} 